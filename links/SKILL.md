---
name: links
description: links
---

Find all the links for all the potential apps/commands I can run locally. Create a file called links.md if they are not created locally at all, and put all the links in that markdown file. If there is a file somewhere, update links.md with those links. Make it non-destructive and merge instead. What this means is, you are just adding to the collection and updating where is needed. Do not delete or remove any links. So for example, if you have an app called outreach, just update the local host link with the new one or the one that is most up to date. Open the links.md file for me somewhere in antigravity because I might need to refer to it multiple times
